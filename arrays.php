<?php 


  # Indexed arrays
  $planes = array('bf109', 'fw190', 'me262' );
  $tanks = ['Tiger I', 'Panzer IV', 'Panther'];
  // echo $planes[1];
  // echo $tanks[2];
  // echo count($planes); //returns number of array elements
  // print_r($tanks); //prints the array
  // var_dump($planes); // prints the array and the data type of its elements

  # Associative arrays
  $people = array('Brad' => 35, 'Ivan' => 29, 'Pepe' => 109);
  $people['Hannah'] = 30;
  // echo $people['Hannah'];
  // print_r($people);

  # MultiDimensional Arrays
  $cars = array(
    array()
  );

?>