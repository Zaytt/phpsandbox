<?php 
  $loggedIn = true;
  if($loggedIn) {
    echo "You're logged in";
  } else {
    echo "You're not logged in";
  }

  #Ternary operator works with the same syntax as other languages
  // (condition) ? (action if true) : (action if false);
?>